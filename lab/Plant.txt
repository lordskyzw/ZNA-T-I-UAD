CREATE MASTER DATASET (check)
- now that we have the merged_dataset.csv, we can use that to find anomalies


CREATE SURF ANOMALIES DATASET (check)

CREATE ULTIMATE ANOMALIES DATASET ()






APP FUNCTIONALITIES:
- forecasting(user_id, period) should return a graph use seaborn and matplotlib, get_anomalies(plan)
